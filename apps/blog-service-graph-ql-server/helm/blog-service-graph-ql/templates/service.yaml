apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-service-graph-ql.fullname" . }}
  labels:
    {{- include "blog-service-graph-ql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blog-service-graph-ql.selectorLabels" . | nindent 4 }}
